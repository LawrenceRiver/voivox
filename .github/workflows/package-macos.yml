name: Package macOS candidate

on:
  workflow_dispatch:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  package:
    name: arm64 package candidate
    runs-on: macos-15
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm test
      - run: npm run typecheck
      - run: swift test
        working-directory: native/macos
      - run: npm run package:mac --workspace=@voivox/desktop
      - run: npm run package:store --workspace=@voivox/chrome-extension
      - run: npm run package:automation --workspace=@voivox/chrome-extension
      - name: Generate SHA-256 checksums
        run: |
          : > SHA256SUMS.txt
          shopt -s nullglob
          artifacts=(
            apps/desktop/release/Vac\ Vox-*.dmg
            apps/desktop/release/Vac\ Vox-*.zip
            apps/chrome-extension/release/Voice-VAC-Store-*.zip
            apps/chrome-extension/release/Voice-VAC-Automation-*.zip
          )
          for artifact in "${artifacts[@]}"
          do
            checksum="$(shasum -a 256 "$artifact" | awk '{print $1}')"
            printf '%s  %s\n' "$checksum" "$(basename "$artifact")" >> SHA256SUMS.txt
          done
      - uses: actions/upload-artifact@v4
        with:
          name: VoiceVac-judge-builds
          path: |
            apps/desktop/release/Voice Vac-*.dmg
            apps/desktop/release/Voice Vac-*.zip
            apps/desktop/release/Voice Vac-*.blockmap
            apps/chrome-extension/release/Voice-VAC-Store-*.zip
            apps/chrome-extension/release/Voice-VAC-Automation-*.zip
            SHA256SUMS.txt
          if-no-files-found: error
      - name: Publish tagged GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          shopt -s nullglob
          artifacts=(
            apps/desktop/release/Vac\ Vox-*.dmg
            apps/desktop/release/Vac\ Vox-*.zip
            apps/chrome-extension/release/Voice-VAC-Store-*.zip
            apps/chrome-extension/release/Voice-VAC-Automation-*.zip
          )
          if gh release view "$GITHUB_REF_NAME" >/dev/null 2>&1; then
            gh release upload "$GITHUB_REF_NAME" \
              "${artifacts[@]}" \
              SHA256SUMS.txt \
              --clobber
          else
            gh release create "$GITHUB_REF_NAME" \
              "${artifacts[@]}" \
              SHA256SUMS.txt \
              --generate-notes \
              --title "Voice Vac $GITHUB_REF_NAME"
          fi
