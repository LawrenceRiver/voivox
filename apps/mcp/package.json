{
  "name": "@voivox/mcp",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "voivox-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node22 --format=esm --banner:js='#!/usr/bin/env node' --outfile=dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.4",
    "@voivox/core": "file:../../packages/core",
    "zod": "^3.24.1"
  }
}
